[pytest]

testpaths = tests

pythonpath = .

python_files = test_*.py

addopts = --alluredir=allure-results --clean-alluredir --headed

filterwarnings =
    ignore::pytest.PytestUnknownMarkWarning

markers =
    only: маркер для запуска конкретного теста